import { FilterByModelPipe } from './filter-by-model-pipe';

describe('FilterByModelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByModelPipe();
    expect(pipe).toBeTruthy();
  });
});
